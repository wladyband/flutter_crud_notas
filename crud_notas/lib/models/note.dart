class Note {
  String noteID;
  String noteTitle;
  DateTime createDateTime;
  DateTime latestEditDateTime;

  Note(
      {this.noteID,
      this.noteTitle,
      this.createDateTime,
      this.latestEditDateTime});
}
